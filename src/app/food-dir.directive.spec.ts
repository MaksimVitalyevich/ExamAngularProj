import { FoodDirDirective } from './food-dir.directive';

describe('FoodDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FoodDirDirective();
    expect(directive).toBeTruthy();
  });
});
